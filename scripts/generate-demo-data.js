/**
 * NEOCARE Doctor Dashboard — Demo Data Generator
 * ────────────────────────────────────────────────
 * This Node.js script generates synthetic demo data and writes
 * it to the /data/ directory.
 *
 * Usage (from project root):
 *   node scripts/generate-demo-data.js
 *
 * Output files:
 *   data/patients.json     — Array of synthetic patient records
 *   data/sensor-data.json  — Keyed object of synthetic sensor readings
 *   data/alerts.json       — Array of synthetic alert records
 *
 * IMPORTANT:
 *   All generated data is entirely synthetic and randomly generated.
 *   No real patient or clinical data is used or produced.
 *   This script is for DEMO and PORTFOLIO purposes only.
 *
 * NOTE: Implementation is planned for a later stage (Stage 3).
 *       This file is a placeholder for the scaffolding.
 */

// TODO (Stage 3): Implement generatePatients(count) → Patient[].
// TODO (Stage 3): Implement generateSensorData(patientIds) → SensorData.
// TODO (Stage 3): Implement generateAlerts(patients) → Alert[].
// TODO (Stage 3): Implement writeDataFiles(patients, sensorData, alerts).
// TODO (Stage 3): Wire up: generatePatients → generateSensorData → generateAlerts → writeDataFiles.
